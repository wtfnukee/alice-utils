[project]
name = "alice-util"
version = "0.1.0"
description = "Alice skill for server monitoring (top, wandb...)"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0.2",
    "psutil>=7.0.0",
    "tqdm>=4.67.1",
    "wandb>=0.19.10",
]
